[project]
name = "BenchmarkPEPS"
requires-python = ">=3.10"
dependencies = [
  "numpy >= 1.23",
  "scipy >= 1.10",
  "sympy >= 1.11",
  "numba >= 0.57",
]

authors = [
  {name = "Olivier Gauthé"},
]
